buildscript {
    repositories {
        jcenter()
        google()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.1.3'
    }
}

ext {
    defaultTargetSdkVersion = 25
    defaultCompileSdkVersion = 26
    defaultBuildToolsVersion = '28.0.0'
    supportLib = "27.1.1"
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}
