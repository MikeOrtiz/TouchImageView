buildscript {
    repositories {
        jcenter()
        google()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.1.0'
    }
}

ext {
    defaultTargetSdkVersion = 25
    defaultCompileSdkVersion = 26
    defaultBuildToolsVersion = '27.0.3'
    supportLib = "27.1.0"
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}
