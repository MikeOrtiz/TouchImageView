buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.0'
    }
}

ext {
    defaultTargetSdkVersion = 29
    defaultCompileSdkVersion = 29
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
